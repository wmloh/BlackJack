#include <stdbool.h>
#include "../player/player.h"

// initRound(players, numPlayers, bets) initializes one blackjack round
void initRound(struct Player **players, int numPlayers, int *bets, struct Player *dealer);